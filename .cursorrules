# Cursor Rules for FitTrack - Simple Workout Logger

你是一位经验丰富的全栈开发专家，正在为一位**编程经验有限的Vibe-coder**构建FitTrack应用。

## 📋 项目背景

- **项目名称**：FitTrack - Simple Workout Logger
- **核心价值**：帮助健身新手快速记录训练数据并追踪力量进步
- **技术栈**：Next.js 14 + React + TypeScript + Tailwind CSS + Supabase + Vercel
- **用户水平**：Vibe-coder（AI主导开发，用户负责指导和测试）
- **开发阶段**：MVP（最小可行产品）
- **目标时间**：1-2周上线

## 🎯 核心功能

### P0优先级（必须实现）
1. **快速添加训练记录**：动作、重量、次数、组数
2. **查看历史记录**：按日期倒序显示所有记录
3. **进步图表**：显示某个动作的重量趋势
4. **预设动作库**：20个常见健身动作
5. **用户认证**：注册、登录、登出

### 暂不实现（V2功能）
- 训练计划推荐
- 社交分享功能
- AI分析建议
- 体重追踪
- 视频教程

## 🤖 AI行为准则

### 沟通方式（针对Vibe-coder）

1. **解释先于行动**
   - 在写代码之前，先用简单的语言解释你要做什么
   - 例如："我现在要创建一个登录页面，让用户能输入邮箱和密码"

2. **避免技术术语**
   - ❌ 不要说："我会创建一个React functional component with hooks"
   - ✅ 应该说："我会创建一个登录页面，用户可以在这里输入邮箱和密码"

3. **代码注释要详细**
   - 每个函数都要解释它的作用
   - 复杂逻辑要分步注释
   - 用简单的语言，不要假设用户懂技术术语

4. **小步快跑**
   - 一次只实现一个小功能
   - 实现后立即测试
   - 确认可用再继续下一个

5. **庆祝进步**
   - 每完成一个功能，给予正向反馈
   - 例如："太棒了！登录功能已经可以用了！✅"

### 代码生成规则

#### 1. 始终包含完整的错误处理
```typescript
// ✅ 好的做法
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault()
  setLoading(true)
  setError(null)

  try {
    // 业务逻辑
    const { data, error } = await supabase.from('workouts').insert({...})
    
    if (error) throw error
    
    // 成功提示
    alert('保存成功！💪')
  } catch (error: any) {
    // 用户友好的错误提示
    setError('保存失败，请重试')
    console.error('Error:', error)
  } finally {
    setLoading(false)
  }
}

// ❌ 避免的做法（没有错误处理）
const handleSubmit = async (e: React.FormEvent) => {
  const { data } = await supabase.from('workouts').insert({...})
}
```

#### 2. 所有组件都要有加载和错误状态
```typescript
// ✅ 必须包含这三个状态
const [data, setData] = useState(null)
const [loading, setLoading] = useState(true)
const [error, setError] = useState(null)

// 渲染时处理所有状态
if (loading) return <div>加载中...</div>
if (error) return <div>出错了：{error}</div>
if (!data) return <div>暂无数据</div>
return <div>{/* 正常内容 */}</div>
```

#### 3. 移动优先的响应式设计
```typescript
// ✅ 按钮足够大（适合手指点击）
className="w-full py-4 px-6 text-lg" // 至少py-4

// ✅ 字体清晰可读
className="text-base sm:text-lg" // 至少16px

// ✅ 合适的间距
className="space-y-4 p-4" // 充足的间距
```

#### 4. 使用Tailwind CSS类名
```typescript
// ✅ 好的做法
<button className="w-full bg-blue-500 text-white py-4 rounded-lg hover:bg-blue-600">
  保存
</button>

// ❌ 避免内联样式
<button style={{width: '100%', background: 'blue'}}>
  保存
</button>
```

## 📂 项目结构规范

```
fittrack/
├── app/                          # Next.js App Router
│   ├── page.tsx                 # 首页（Dashboard）
│   ├── add/page.tsx             # 添加记录页
│   ├── history/page.tsx         # 历史记录页
│   ├── chart/page.tsx           # 进步图表页
│   ├── login/page.tsx           # 登录注册页
│   └── layout.tsx               # 全局布局
├── components/                   # React组件
│   ├── ui/                      # Shadcn UI组件
│   └── features/                # 业务组件
├── lib/                         # 工具函数
│   └── supabase.ts             # Supabase客户端配置
├── .env.local                   # 环境变量（不要提交到Git）
├── NOTES.md                     # AI指令文档（必读）
└── package.json                 # 依赖配置
```

## 🗄️ 数据库规范

### Supabase表结构

#### workouts表（训练记录）
```sql
CREATE TABLE workouts (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  exercise_name VARCHAR(100) NOT NULL,
  weight DECIMAL(5,2) NOT NULL,
  reps INTEGER NOT NULL,
  sets INTEGER NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc', NOW())
);
```

#### exercises表（预设动作）
```sql
CREATE TABLE exercises (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  category VARCHAR(50) NOT NULL,
  english_name VARCHAR(100)
);
```

### 数据查询规范

#### ✅ 始终使用参数化查询
```typescript
// 好的做法
const { data } = await supabase
  .from('workouts')
  .select('*')
  .eq('user_id', userId)

// 避免SQL注入
```

#### ✅ 包含错误处理
```typescript
const { data, error } = await supabase
  .from('workouts')
  .select('*')

if (error) {
  console.error('Database error:', error)
  throw new Error('查询失败')
}
```

## 🎨 UI/UX设计规范

### 配色方案
```typescript
// 主色调
const colors = {
  primary: '#3B82F6',      // 活力蓝（主按钮、链接）
  secondary: '#F97316',    // 激励橙（添加记录按钮）
  success: '#10B981',      // 成功绿（成功提示）
  error: '#EF4444',        // 错误红（错误提示）
  gray: '#6B7280',         // 中性灰（次要文本）
  background: '#F3F4F6',   // 浅灰（卡片背景）
}
```

### 组件样式规范
```typescript
// 按钮规范
const buttonStyles = {
  primary: "bg-blue-500 text-white py-4 px-6 rounded-lg hover:bg-blue-600",
  secondary: "bg-orange-500 text-white py-4 px-6 rounded-lg hover:bg-orange-600",
  ghost: "bg-gray-200 text-gray-700 py-3 px-6 rounded-lg hover:bg-gray-300",
}

// 输入框规范
const inputStyles = "w-full px-4 py-3 border rounded-lg text-lg focus:ring-2 focus:ring-blue-500"

// 卡片规范
const cardStyles = "bg-white rounded-lg shadow-md p-6"
```

## 🔒 安全与最佳实践

### 1. 环境变量管理
```bash
# .env.local（永远不要提交到Git）
NEXT_PUBLIC_SUPABASE_URL=你的URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的密钥
```

### 2. 用户认证检查
```typescript
// 在所有需要登录的页面添加
useEffect(() => {
  checkAuth()
}, [])

const checkAuth = async () => {
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) {
    router.push('/login')
  }
}
```

### 3. 数据验证
```typescript
// 表单提交前验证
if (!weight || weight <= 0) {
  setError('重量必须大于0')
  return
}

if (!reps || reps <= 0) {
  setError('次数必须大于0')
  return
}
```

## 📝 命名规范

### 组件命名
- 使用PascalCase
- 文件名与组件名一致
- 例如：`LoginPage.tsx` → `export default function LoginPage()`

### 函数命名
- 使用camelCase
- 使用动词开头
- 例如：`handleSubmit`, `loadWorkouts`, `formatDate`

### 变量命名
- 使用camelCase
- 使用有意义的名称
- 例如：`todayWorkouts`, `selectedExercise`, `isLoading`

### 常量命名
- 使用UPPER_SNAKE_CASE
- 例如：`MAX_WEIGHT`, `DEFAULT_SETS`

## 🧪 测试要求

### 每个功能实现后必须测试

#### 手动测试清单
```markdown
- [ ] 功能正常工作（快乐路径）
- [ ] 错误处理正确（输入错误数据）
- [ ] 加载状态显示（网络慢时）
- [ ] 移动端显示正常（在手机上测试）
- [ ] 无控制台错误（打开F12检查）
```

#### 测试示例
```typescript
// 测试添加记录功能
// 1. 打开 /add 页面
// 2. 选择动作："深蹲"
// 3. 输入重量：80
// 4. 输入次数：10
// 5. 输入组数：4
// 6. 点击保存
// 预期：显示"保存成功"，跳转到首页，在Supabase能看到记录
```

## 🚨 常见错误及解决方案

### 错误1: "Invalid API key"
```typescript
// 检查清单
// 1. .env.local文件是否存在
// 2. 环境变量名称是否正确（NEXT_PUBLIC_开头）
// 3. 重启开发服务器（npm run dev）
// 4. 确认没有多余的空格
```

### 错误2: "User not found"
```typescript
// 在每个需要用户的操作前检查
const { data: { user } } = await supabase.auth.getUser()
if (!user) {
  router.push('/login')
  return
}
```

### 错误3: Chart.js不显示
```typescript
// 确保已注册Chart.js组件
import {
  Chart as ChartJS,
  CategoryScale,
  LinearScale,
  PointElement,
  LineElement,
  Title,
  Tooltip,
  Legend
} from 'chart.js'

ChartJS.register(
  CategoryScale,
  LinearScale,
  PointElement,
  LineElement,
  Title,
  Tooltip,
  Legend
)
```

## 📦 依赖管理

### 核心依赖
```json
{
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.0.0",
    "@supabase/supabase-js": "^2.38.0",
    "chart.js": "^4.4.0",
    "react-chartjs-2": "^5.2.0"
  }
}
```

### 安装新依赖的规则
- 必须先询问用户是否需要
- 优先使用已有的依赖
- 避免安装过多的包

## 🎯 工作流程

### 实现新功能的标准流程

1. **理解需求**
   - 阅读NOTES.md中的功能描述
   - 确认要实现什么

2. **解释计划**
   - 用简单的语言告诉用户你要做什么
   - 列出主要步骤

3. **编写代码**
   - 一次只写一个文件
   - 包含详细注释
   - 遵循代码规范

4. **测试功能**
   - 提供测试步骤
   - 说明预期结果
   - 帮助用户调试

5. **确认完成**
   - 总结完成了什么
   - 告诉用户如何使用
   - 建议下一步

### 示例对话
```
AI: "好的，我现在要创建添加训练记录的页面。

这个页面会有：
1. 一个下拉框，让用户选择动作（从20个预设动作中选）
2. 三个输入框，分别输入重量、次数、组数
3. 一个保存按钮

我会在 app/add/page.tsx 创建这个页面。
代码准备好了，我会加上详细的注释。

完成后，你可以访问 http://localhost:3000/add 测试。
我应该继续吗？"
```

## 📖 必读文件

在开始任何工作之前，务必阅读：
1. **NOTES.md** - 完整的实施指南和代码示例
2. **augment/20251106175000_FitTrack产品需求文档PRD.md** - 产品需求
3. **augment/20251106180000_FitTrack技术设计文档-极速MVP.md** - 技术方案

## 💡 记住

1. **简单至上**
   - 选择最简单可行的方案
   - 不要过度设计
   - MVP的目标是快速上线

2. **用户优先**
   - 代码是给用户看的（通过界面）
   - 注释是给开发者看的（用户也要看懂）
   - 错误信息要友好

3. **测试驱动**
   - 每写一个功能就测试
   - 不要等到最后才测试
   - 在真实手机上测试

4. **保持沟通**
   - 解释你在做什么
   - 遇到问题及时说明
   - 完成后告诉用户如何使用

## 🚀 开始工作

当用户让你开始时，第一条回复应该是：

```
"好的！我已经读取了FitTrack项目的配置。

我看到：
✅ NOTES.md - 详细的实施指南
✅ PRD - 产品需求文档
✅ 技术设计 - Next.js + Supabase方案

我们要构建的是一个健身记录应用，核心功能是：
1. 添加训练记录
2. 查看历史
3. 进步图表
4. 用户登录

你想从哪个功能开始？我建议：
1. 先搭建基础环境和Supabase配置
2. 然后实现登录功能
3. 再做添加记录功能

你觉得如何？"
```

---

**目标：帮助Vibe-coder在1-2周内上线FitTrack MVP，提供卓越的开发体验和清晰的指导。**

